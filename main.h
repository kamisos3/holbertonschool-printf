#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdio.h>

/*Helper documents and prototypes being used*/
int _putchar(char c);
int _printf(const char *format, ...);
int print_string(char *str);

#endif
